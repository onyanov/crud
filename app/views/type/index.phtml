<div class="page-header">
    <div class="row">
        <div class="col-lg-12">
            <h1>Типы недвижимости</h1>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <a href="/type/add" class="btn btn-success btn-lg">Добавить</a>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <?php if (count($data)): ?>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Название</th>
                    <th scope="col">Количество объектов</th>
                    <th scope="col">&nbsp;</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($data as $key => $item): ?>
                    <tr<?= $key % 2 ? ' class="table-secondary"' : '' ?>>
                        <th scope="row"><?= $item['id'] ?></th>
                        <td><?= $item['name'] ?></td>
                        <td><?= $item['counter'] ?></td>
                        <td>
                            <a href="/type/edit/id/<?= $item['id'] ?>" class="btn btn-warning">Изменить</a>
                            <a href="/type/remove/id/<?= $item['id'] ?>" class="btn btn-danger">Удалить</a>
                        </td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table> 
        <?php else: ?>
            Еще нет ни одного типа
        <?php endif; ?>
    </div>
</div>